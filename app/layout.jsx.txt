import './globals.css'

export const metadata = {
  title: 'Runnoz Performance',
  description: 'Performance tracking',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}